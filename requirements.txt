PySide6>=6.5

# Audio-reactive bars: taps the current output device via the
# PulseAudio/PipeWire "monitor" source. Needs the system `libpulse` (already
# present on any PipeWire/PulseAudio desktop) and `numpy`; `soundcard` is
# the backend client. Without these, the drips simply stay at their quiet
# idle length (no crash).
numpy>=1.24
soundcard>=0.4.1
